name: gp-ssm
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pytorch>=2.2.0,<2.3.0
  - numpy>=1.26,<2
  - pyyaml>=6.0
  - typer>=0.12
  - rich>=13.7
  - pytest>=8.3
  - black>=24.4
  - ruff>=0.5
  - mypy>=1.10
  - pip:
      - pyro-ppl>=1.9.1,<1.10
      - -e .
